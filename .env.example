# ==========================================
# GAZEBOT ENVIRONMENT VARIABLES
# ==========================================
# Rename this file to .env for local testing.
# For production, add these as Repository Secrets in GitHub Actions.

# --- Database (MongoDB Atlas) ---
# Your MongoDB connection string. 
# Make sure to replace <username> and <password> with your database user credentials.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/gazebot?retryWrites=true&w=majority

# --- Image Storage (Cloudinary) ---
# Used to host baseline and diff images. 
# You can find these on your Cloudinary Dashboard under "Product Environment Credentials".
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# --- Email Notifications (Nodemailer) ---
# SMTP credentials to send the daily alerts.
# If using Gmail, you MUST use an "App Password", not your standard account password.
# If using a service like Resend or SendGrid, update the HOST and PORT accordingly.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password_or_smtp_key
